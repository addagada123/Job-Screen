# Job Screen Backend

- All backend code is now in `backend/server/`
- Place your .env file in `backend/`
- Run backend with: `node backend/server/server.js`
- Uploads are stored in `backend/uploads/`
- Serves frontend from `frontend/public/` for production
